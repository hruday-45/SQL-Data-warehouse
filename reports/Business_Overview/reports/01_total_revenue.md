# 📈 Revenue Summary (Grand Total)

---

## 🧠 Business Question
What is the total revenue generated by the platform, and what does it indicate about overall sales performance?

---

## 🎯 Why This Matters
- Provides a **single source of truth** for total revenue
- Helps leadership assess **overall business scale**
- Serves as a baseline metric for **growth, forecasting, and benchmarking**
- Used in executive dashboards and financial reporting

---

## 🧩 Data Sources
| Layer | View |
|------|------|
| Gold | `gold.fact_sales` |

---

## 🛠️ Logic Overview
- Aggregates total product revenue across all completed sales
- Counts distinct orders to avoid duplication
- Calculates Average Order Value (AOV) as revenue per order

---

## 📌 Key Metrics
| Metric | Value |
|------|------|
| **Total Revenue** | **14,803,808.78** |
| **Total Orders** | **98,666** |
| **Average Order Value (AOV)** | **150.04** |

---

## 🔍 Key Insights
- The platform has generated **14.8M** in total revenue
- An AOV of **150** indicates a **mid-ticket marketplace**
- Revenue is distributed across a **large order base**, reducing dependency on individual transactions

---

## 📊 Output

| grand_total_revenue | total_orders | average_order_value |
|--------------------|--------------|---------------------|
| 14,803,808.78 | 98,666 | 150.04 |

---

## 🧪 Data Quality Checks
- ✔ Ensured no duplicate `order_key` values
- ✔ Revenue values are non-negative
- ✔ Aggregation performed at the correct grain (order level)

---

## 🧱 SQL Reference

```sql
-- Total Revenue (Grand Total)
SELECT 
    SUM(total_product_value) AS grand_total_revenue,
    COUNT(DISTINCT order_key) AS total_orders,
    SUM(total_product_value) / COUNT(DISTINCT order_key) AS average_order_value
FROM gold.fact_sales;
